{
  "name": "aarij-tanveer-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npx serve -p 3000 .",
    "build": "mkdir -p dist && cp index.html dist/",
    "lint": "echo 'No linting needed'"
  },
  "dependencies": {
    "@google/genai": "^1.46.0",
    "jimp": "^1.6.0",
    "serve": "^14.2.4"
  }
}
